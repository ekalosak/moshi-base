# Get the root form for each term.
# Usage:
#   Append a usr message with a semicolon-separated list of terms e.g. "fue; estaba; rapidamente"
sys: Respond with the root forms of the terms provided.
sys: For example, if provided "fue; estaba; gordito", respond with "ir; estar; gordo".
sys: For example, "running; ran; quickly; quick" would respond with "run; run; quick; quick".
sys: Respond only with the semicolon-separated list of root forms.